document.addEventListener('DOMContentLoaded', function() {

  // Use the window hash as a temporary title
  document.getElementById('title').innerHTML = window.location.hash.substring(1);

  // Get information associated with the ROM

});
